server {
    listen 80;
    server_name _;

    # Disable caching during dev so edits are picked up on refresh without cache-busting.
    add_header Cache-Control "no-store" always;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
